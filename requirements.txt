# Scanner dependencies (scanner/ only — the skill itself needs nothing).
requests>=2.31
playwright>=1.40
mplfinance>=0.12        # fallback self-drawn charts (no TradingView login / CHART_MODE=mpl)
# playwright is used for: (a) the Deepvue backend, and (b) real TradingView chart screenshots
# (logged-in, with your OWN indicators — needs a local tv_cookies.json, git-ignored). After install:
#   playwright install chromium
# No TradingView login? charts auto-fall back to mplfinance self-draw. No charts at all? CHARTS=0.
