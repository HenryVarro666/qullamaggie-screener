# ── Credentials & local state — NEVER commit ──
cookies.json
tokens.json
symbols2_map.json
.deepvue/
*.local.json

# ── Scan outputs (reports + generated TradingView chart PNGs) ──
扫描结果/
qmag-scans/
*-scans/
scans/
charts/

# ── Python ──
__pycache__/
*.pyc
.venv/
venv/

# ── OS ──
.DS_Store
Thumbs.db
